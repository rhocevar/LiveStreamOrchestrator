# ===========================================
# SERVER CONFIGURATION
# ===========================================
NODE_ENV=development
PORT=3001

# ===========================================
# DATABASE CONFIGURATION (PostgreSQL)
# ===========================================
# PostgreSQL connection string format:
# postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
#
# Example for local PostgreSQL:
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/favorited?schema=public"
#
# Example for Prisma Postgres (local dev):
# DATABASE_URL="prisma+postgres://localhost:51213/?api_key=YOUR_API_KEY"
#
# For production, use your hosted PostgreSQL URL
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"

# ===========================================
# LIVEKIT CONFIGURATION
# ===========================================
# LiveKit Cloud credentials (get from: https://cloud.livekit.io)
LIVEKIT_URL=https://your-project.livekit.cloud
LIVEKIT_API_KEY=your-api-key-here
LIVEKIT_API_SECRET=your-api-secret-here

# For local development with LiveKit server:
# LIVEKIT_URL=ws://localhost:7880
# LIVEKIT_API_KEY=devkey
# LIVEKIT_API_SECRET=secret

# Access token expiration in hours (default: 24)
TOKEN_EXPIRATION_HOURS=24

# ===========================================
# REDIS CONFIGURATION (for webhook queue)
# ===========================================
# Redis connection URL format:
# redis://[username][:password]@host:port[/database]
#
# Example for local Redis:
# REDIS_URL=redis://localhost:6379
#
# Example for Redis Cloud or hosted Redis:
# REDIS_URL=redis://username:password@your-redis-host:6379
#
# For production, use your hosted Redis URL (Redis Cloud, AWS ElastiCache, etc.)
REDIS_URL=redis://localhost:6379

# Webhook queue worker concurrency (default: 10)
# Controls how many webhook jobs can be processed simultaneously
WEBHOOK_QUEUE_CONCURRENCY=10

# ===========================================
# CLEANUP JOBS CONFIGURATION
# ===========================================
# Livestream reconciliation job interval in minutes (default: 10)
# Reconciles database state with LiveKit rooms to handle missed webhooks
RECONCILIATION_INTERVAL_MINUTES=10
